workflows:
  ios-workflow:
    name: LinkVault iOS + Share Extension
    max_build_duration: 90
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: codemagic
    environment:
      groups:
        - code_signing
      vars:
        BUNDLE_ID: "com.linkvaultq8.app"
        SHARE_EXTENSION_BUNDLE_ID: "com.linkvaultq8.app.ShareExtension"
        APP_GROUP_ID: "group.com.linkvaultq8.shared"
        USE_APP_GROUPS: "true"
        XCODE_WORKSPACE: "ios/App/App.xcworkspace"
        XCODE_SCHEME: "App"
        IOS_DEPLOYMENT_TARGET: "14.0"
      node: 20.18.0
      xcode: latest
      cocoapods: default
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
          include: true
          source: true
    scripts:
      - name: Install npm dependencies
        script: |
          npm config set registry https://registry.npmjs.org/
          npm ci --include=dev --no-audit --no-fund --prefer-online
      - name: Add iOS platform if missing
        script: |
          test -d ios || npx --no-install cap add ios
      - name: Generate iOS app assets
        script: |
          bash scripts/generate-ios-assets.sh
      - name: Sync web assets and native plugins
        script: |
          npx --no-install cap sync ios
      - name: Add native iOS Share Extension
        script: |
          gem install xcodeproj --no-document
          ruby scripts/add-ios-share-extension.rb
      - name: CocoaPods installation
        script: |
          cd ios/App
          pod install
      - name: Set up keychain for code signing
        script: |
          keychain initialize
      - name: Fetch signing files for app and share extension
        script: |
          app-store-connect fetch-signing-files "$BUNDLE_ID" \
            --type IOS_APP_STORE \
            --create
          app-store-connect fetch-signing-files "$SHARE_EXTENSION_BUNDLE_ID" \
            --type IOS_APP_STORE \
            --create
      - name: Add certificates to keychain
        script: |
          keychain add-certificates
      - name: Apply provisioning profiles to Xcode project
        script: |
          xcode-project use-profiles --project ios/App/App.xcodeproj
      - name: Increment build number
        script: |
          cd ios/App
          agvtool new-version -all $BUILD_NUMBER || true
      - name: Build ipa for TestFlight
        script: |
          xcode-project build-ipa \
            --workspace "$XCODE_WORKSPACE" \
            --scheme "$XCODE_SCHEME"
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - ios/App/App.xcodeproj/project.pbxproj
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
        submit_to_app_store: false

  web-check:
    name: LinkVault Web Static Check
    max_build_duration: 20
    instance_type: linux_x2
    environment:
      node: 20.18.0
    scripts:
      - name: Install npm dependencies
        script: |
          npm config set registry https://registry.npmjs.org/
          npm ci --include=dev --no-audit --no-fund --prefer-online
      - name: Check backend syntax
        script: |
          node --check backend/server.js
    artifacts:
      - www/**
      - backend/**
